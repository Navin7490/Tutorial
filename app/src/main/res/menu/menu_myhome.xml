<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/Menu_my_notificatin"
        android:icon="@drawable/ic__notificationshome"
        app:showAsAction="ifRoom|collapseActionView"
        tools:ignore="MenuTitle" />
    <item android:id="@+id/Menu_my_flage"
        android:icon="@drawable/ic_flaghome"
        app:showAsAction="ifRoom|collapseActionView"
        tools:ignore="MenuTitle" />
    <item android:id="@+id/Menu_my_profile"
        android:icon="@drawable/ic_accounthome"
        app:showAsAction="ifRoom"

        tools:ignore="MenuTitle" />
</menu>